# Client Routes (Next.js)

- `/` – landing (placeholder)
- `/engine-status` – pings engine `/health`
- `/studio` – loads UI Builder; internal components live under `components/ui/ui-builder/*`

> Add new routes here as they are created and note which engine endpoints they use.
